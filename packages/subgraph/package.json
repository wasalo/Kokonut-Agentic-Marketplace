{
  "name": "@kokonut/subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy kokonut-sepolia --node https://api.studio.thegraph.com/deploy/ --deploy-key $THEGRAPH_KEY --version-label v0.1.0",
    "create-local": "graph create --node http://localhost:8020 kokonut-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 kokonut-sepolia"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.96.0",
    "@graphprotocol/graph-ts": "^0.37.0"
  }
}
